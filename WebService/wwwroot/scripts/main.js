require(["knockout", "jquery", "dataservice"], function (ko, $, dat) {
    var vm = (function () {
        var self = this;
           
        return {
        };
    })();

    ko.applyBindings(vm);
});